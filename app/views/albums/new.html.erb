<%
   params['a'] = @artist
   params['n'] = @name
   params['i'] = @image
%>

<div class="container">

  <div class="col-sm-4 col-sm-offset-4">
      <p><%= image_tag(@large_image, width: '100%') %></p>
      <h3><%= @name %></h3>
      <p class="h5"><%= @release_date %>, <%= @artist.upcase %></p>

      <%= render 'form' %>

  </div>

</div>
