<% provide(:html_class, 'top5') %>
<% provide(:body_class, 'top5') %>
<% provide(:body_style, "style=background-image:url(#{image_url(randomized_image)})") %>
<% provide(:title, 'Your Top 5 Albums of All Time') %>

<div class="container">

  <div class="box">
    <div class="text-center">

      <p id="notice" class="hidden"><%= notice %></p>

      <h1 class="page-heading">Your Top 5 Albums</h1>

      <%= render 'albums' %>

    </div>
  </div>
</div>