<% provide(:html_class, 'title-screen') %>

<div class="container text-center">
  <div class="box">
    <div class="single-column">
      <h1 class="brand"><span>TOP</span> <span>5</span> <span>ALBUMS</span></h1>
      <% if logged_in? %>
          <p><%= link_to 'ENTER', your_top_5_path, class: "btn btn-default btn-lg" %></p>
      <% else %>
          <p><%= link_to 'ENTER', :register, class: "btn btn-default btn-lg" %></p>
      <% end %>
    </div>
  </div>
</div>

